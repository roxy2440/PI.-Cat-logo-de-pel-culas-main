import { Validator, Document } from './Validator';
import { AnySchema, ErrorObject } from 'ajv';
export declare abstract class BaseValidator<T extends Document> implements Validator<T> {
    private readonly ajv;
    protected constructor(schemas: AnySchema[]);
    protected abstract getSchemaId(document: T): string;
    verify(document: T): Promise<ErrorObject[]>;
    private verifySchema;
}
