import { BaseValidator } from './BaseValidator';
import { Har } from '@har-sdk/core';
export declare class HarValidator extends BaseValidator<Har> {
    private readonly SCHEMA_ID;
    constructor();
    protected getSchemaId(_: Har): string;
}
