import { BaseFormatter } from './BaseFormatter';
import { ErrorObject } from 'ajv';
export declare class ArrayFormatter extends BaseFormatter {
    protected readonly supportedKeywords: Set<string>;
    format(error: ErrorObject<'maxItems' | 'minItems', {
        limit: number;
    }> | ErrorObject<'uniqueItems', {
        i: number;
        j: number;
    }>): string;
}
