import { BaseFormatter } from './BaseFormatter';
import { ErrorObject } from 'ajv';
export declare class ObjectFormatter extends BaseFormatter {
    protected readonly supportedKeywords: Set<string>;
    format(error: ErrorObject<'additionalProperties', {
        additionalProperty: string;
    }> | ErrorObject<'required', {
        missingProperty: string;
    }> | ErrorObject<'minProperties' | 'maxProperties', {
        limit: number;
    }>): string;
}
