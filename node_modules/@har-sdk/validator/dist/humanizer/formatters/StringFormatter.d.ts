import { BaseFormatter } from './BaseFormatter';
import { ErrorObject } from 'ajv';
export declare class StringFormatter extends BaseFormatter {
    protected readonly supportedKeywords: Set<string>;
    format(error: ErrorObject<'minLength' | 'maxLength', {
        limit: number;
    }> | ErrorObject<'pattern', {
        pattern: string;
    }> | ErrorObject<'format', {
        format: string;
    }>): string;
}
