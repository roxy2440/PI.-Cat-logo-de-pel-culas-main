import { Formatter } from './Formatter';
import { ErrorObject } from 'ajv';
export declare abstract class BaseFormatter implements Formatter {
    protected readonly supportedKeywords: Readonly<Set<string>>;
    abstract format(error: ErrorObject): string | undefined;
    canProcessKeyword(keyword: string): boolean;
}
