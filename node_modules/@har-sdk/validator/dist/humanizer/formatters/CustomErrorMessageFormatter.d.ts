import { Formatter } from './Formatter';
import { ErrorObject } from 'ajv';
export declare class CustomErrorMessageFormatter implements Formatter {
    canProcessKeyword(keyword: string): boolean;
    format(error: ErrorObject<'errorMessage'>): string;
}
