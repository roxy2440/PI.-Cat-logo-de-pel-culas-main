import { BaseFormatter } from './BaseFormatter';
import { ErrorObject } from 'ajv';
export declare class TypeFormatter extends BaseFormatter {
    protected readonly supportedKeywords: Set<string>;
    format(error: ErrorObject<'enum', {
        allowedValues: any[];
    }> | ErrorObject<'type', {
        type: string | string[];
    }> | ErrorObject<'const', {
        allowedValue: any;
    }>): string;
}
