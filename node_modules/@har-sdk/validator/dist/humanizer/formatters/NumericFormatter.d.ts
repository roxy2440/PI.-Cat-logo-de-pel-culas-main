import { BaseFormatter } from './BaseFormatter';
import { ErrorObject } from 'ajv';
export declare class NumericFormatter extends BaseFormatter {
    protected readonly supportedKeywords: Set<string>;
    format(error: ErrorObject<'minimum' | 'maximum' | 'exclusiveMinimum' | 'exclusiveMaximum', {
        limit: number;
    }>): string;
}
