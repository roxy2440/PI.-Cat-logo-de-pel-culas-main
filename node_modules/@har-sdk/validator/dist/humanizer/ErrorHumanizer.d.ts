import { HumanizedError } from './HumanizedError';
import { ErrorObject } from 'ajv';
export declare class ErrorHumanizer {
    private readonly formatters;
    humanizeErrors(errors: ErrorObject[]): HumanizedError[];
    humanizeError(error: ErrorObject): HumanizedError;
    humanizeErrorMessage(error: ErrorObject): Omit<HumanizedError, 'originalError'>;
    private formatLocation;
}
