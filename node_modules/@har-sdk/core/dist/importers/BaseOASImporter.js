import { BaseImporter } from './BaseImporter';
export class BaseOASImporter extends BaseImporter {
    constructor() {
        super();
    }
    fileName({ doc, format }) {
        var _a;
        if (typeof ((_a = doc === null || doc === void 0 ? void 0 : doc.info) === null || _a === void 0 ? void 0 : _a.title) === 'string' &&
            typeof doc.info.version === 'string') {
            return `${[doc.info.title, doc.info.version]
                .map((x) => x.trim())
                .join(' ')}.${format}`;
        }
    }
}
//# sourceMappingURL=BaseOASImporter.js.map