import { Doc, DocType, Spec, DocFormat } from './Spec';
import { Importer } from './Importer';
export declare class SpecImporter implements Importer<DocType> {
    private readonly importers;
    constructor(importers?: ReadonlyArray<Importer<DocType>>);
    import<TDocType extends DocType, TDoc = Doc<TDocType>>(value: string, format?: DocFormat): Promise<Spec<TDocType, TDoc> | undefined>;
}
